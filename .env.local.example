# GCloud SSH 隧道配置模板
# 复制此文件为 .env.local，然后填入你的实际值
# DO NOT commit .env.local - 它包含敏感信息

# GCE 实例配置
# 获取方式: gcloud compute instances list
GCLOUD_INSTANCE=instance-name-here
GCLOUD_ZONE=zone-here  # 例: asia-east1-b, us-central1-a

# K8s API 服务器配置
# 内部 IP 地址 (通常是 kubeadm 的默认网络 IP)
GCLOUD_REMOTE_HOST=10.0.0.0

# 可选: 端口配置 (默认值已设置)
# GCLOUD_REMOTE_PORT=6443
# GCLOUD_LOCAL_PORT=6443

# KubeMinds 加密主密钥
# 用于解密 config.yaml 中 "enc:aes256:..." 格式的 API Key
# 生成方式: openssl rand -hex 32
# 使用方式: source .env.local && make encrypt-key KEY=<api-key>
KUBEMINDS_MASTER_KEY=your-64-hex-chars-master-key-here

# LLM 配置 (如需要，也可直接写入 config.yaml)
# OPENAI_API_KEY=sk-xxx
# GEMINI_API_KEY=xxx
