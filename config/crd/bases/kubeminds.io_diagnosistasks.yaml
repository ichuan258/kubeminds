---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: diagnosistasks.kubeminds.io
spec:
  group: kubeminds.io
  names:
    kind: DiagnosisTask
    listKind: DiagnosisTaskList
    plural: diagnosistasks
    singular: diagnosistask
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiagnosisTask is the Schema for the diagnosistasks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiagnosisTaskSpec defines the desired state of DiagnosisTask
            properties:
              alertContext:
                description: AlertContext provides context about the alert that triggered
                  this diagnosis
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels associated with the alert (e.g., severity=critical,
                      reason=OOMKilled)
                    type: object
                  name:
                    description: Name of the alert (e.g., KubePodCrashLooping)
                    type: string
                type: object
              approved:
                description: Approved indicates whether the diagnosis actions are
                  approved by a human
                type: boolean
              policy:
                description: Policy controls the diagnosis execution
                properties:
                  maxSteps:
                    default: 10
                    description: MaxSteps is the maximum number of agent steps allowed
                    type: integer
                type: object
              target:
                description: Target identifies the resource to diagnose
                properties:
                  kind:
                    description: Kind of the target resource (e.g. Pod, Deployment)
                    type: string
                  name:
                    description: Name of the target resource (e.g. Pod name)
                    type: string
                  namespace:
                    description: Namespace of the target resource
                    type: string
                type: object
            required:
            - target
            type: object
          status:
            description: DiagnosisTaskStatus defines the observed state of DiagnosisTask
            properties:
              checkpoint:
                description: Checkpoint stores the intermediate findings for crash
                  recovery
                items:
                  description: Finding represents a key discovery made during the
                    diagnosis process
                  properties:
                    step:
                      description: Step index in the diagnosis process
                      type: integer
                    summary:
                      description: Summary of the tool output generated by LLM
                      type: string
                    timestamp:
                      description: Timestamp of the finding
                      type: string
                    toolArgs:
                      description: Arguments passed to the tool
                      type: string
                    toolName:
                      description: Name of the tool executed
                      type: string
                  required:
                  - step
                  type: object
                type: array
              history:
                description: History logs the agent's actions (for debugging/audit)
                items:
                  type: string
                type: array
              matchedSkill:
                description: MatchedSkill indicates the name of the skill matched
                  for this task
                type: string
              phase:
                description: Phase represents the current stage of diagnosis
                enum:
                - Pending
                - Running
                - WaitingApproval
                - Completed
                - Failed
                type: string
              report:
                description: Report contains the final diagnosis results
                properties:
                  rootCause:
                    description: RootCause identified by the agent
                    type: string
                  suggestion:
                    description: Suggestion for remediation
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
